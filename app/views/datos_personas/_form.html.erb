<%= form_for(datos_persona) do |f| %>
  <% if datos_persona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(datos_persona.errors.count, "error") %> prohibited this datos_persona from being saved:</h2>

      <ul>
      <% datos_persona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :country %>
    <%= f.text_field :country %>
  </div>

  <div class="field">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label :receive_email %>
    <%= f.check_box :receive_email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
